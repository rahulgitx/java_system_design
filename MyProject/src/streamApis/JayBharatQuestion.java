package streamApis;

import java.util.*;
import java.util.stream.Collectors;

public class JayBharatQuestion {
    public static void main(String[] args) {
        // sort the given list of number by frequency of there occurences;
        List<Integer> list = Arrays.asList(1,1,2,3,4,5,5);
//        List<List<Integer>> res = list.stream()
//                .collect(Collectors.groupingBy(x->x, Collectors.counting()))
//                .entrySet().stream().sorted((a, b) -> Math.toIntExact(a.getValue() - b.getValue())).collect(Collectors.toList());


    }
}
